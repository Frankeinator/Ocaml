#pragma once
#include <vector>
#include <string>

class Instructions
{
    static int add(std::vector<std::string>);
    static int sub(std::vector<std::string>);
    static int div(std::vector<std::string>);
    static int mult(std::vector<std::string>);
    static int mod(std::vector<std::string>);
};

